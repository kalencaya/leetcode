package cn.sliew.leetcode.contest.weekly256

/**
  * 1986. Minimum Number of Work Sessions to Finish the Tasks
  */
object MinimumNumberofWorkSessionstoFinishtheTasks1986ScalaExample {

    def minSessions(tasks: Array[Int], sessionTime: Int): Int = {
        ???
    }

    def main(args: Array[String]): Unit = {
        val tasks = Array(1, 2, 3, 4, 5)
        val sessionTime = 15
        val result = minSessions(tasks, sessionTime)
        println(result)
    }
}
